import { BaseEdge, type BaseEdgeProps } from "@xyflow/react";

const TransactionTimeEdge = ({ ...restProps }: BaseEdgeProps) => {
  return <BaseEdge {...restProps} />;
};

export default TransactionTimeEdge;
